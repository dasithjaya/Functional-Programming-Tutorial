object q1_and_2 extends App {
	var k,i,j = 2;
	var m,n = 5;
	var f = 12.0f;
	var g = 4.0f;
	var c = 'X';

	println(k + 12 * m)
	println(m/j)
	println(n%j)
	println(m/j*j)
	//println(++i * n)
	//We can not use increment operator (++) in scala.
	//But we can modify it

	i = i + 1; 
	println(i * n)
}